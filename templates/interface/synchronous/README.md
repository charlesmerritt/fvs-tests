# Synchronous tests

Add tests that execute one FVS case at a time.

Each test should identify the case and FVS variant, use an isolated working directory, retain useful failure artifacts, and compare normalized output with the expected result.
